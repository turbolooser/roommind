{
  "config": {
    "step": {
      "user": {
        "title": "RoomMind",
        "description": "Set up RoomMind to manage room climate. Rooms are configured in the RoomMind panel after setup."
      }
    },
    "abort": {
      "already_configured": "RoomMind is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "RoomMind options",
        "description": "Configure vacation behaviour and the AC inverter setpoint strategy.",
        "data": {
          "vacation_action": "Vacation heating behaviour",
          "vacation_frost_temp": "Frost protection temperature (°C)",
          "ac_setpoint_strategy": "AC setpoint strategy",
          "ac_cool_offset_max": "Max cooling offset (K)",
          "ac_heat_offset_max": "Max AC heating offset (K)",
          "idle_off_after_minutes": "Turn device off after idle (min, 0=never)",
          "idle_setback_offset": "Idle setback offset (K)",
          "demand_control_enabled": "Compressor-group demand control",
          "demand_select_entities": "Demand-control select entities",
          "demand_min": "Min demand (%)",
          "demand_max": "Max demand (%)",
          "demand_hysteresis": "Demand hysteresis (%)",
          "demand_min_hold_minutes": "Min hold between demand changes (min)",
          "demand_down_hold_minutes": "Demand step-down hold (min)",
          "pv_boost_enabled": "PV surplus boost",
          "pv_surplus_sensor": "PV surplus sensor (W, positive = surplus)",
          "pv_surplus_min_w": "Min surplus to boost (W)",
          "pv_surplus_min_duration_minutes": "Sustained for (min)",
          "pv_battery_soc_sensor": "Battery state-of-charge sensor (%, optional)",
          "pv_battery_soc_min": "Min battery SoC to boost (%)",
          "pv_boost_cool_percent": "Boost while cooling (%-points)",
          "pv_boost_heat_percent": "Boost while heating (%-points)"
        },
        "data_description": {
          "vacation_action": "\"Setback\" heats to the vacation temperature. \"Off\" disables active heating (frost protection only); cooling continues to track eco cool.",
          "vacation_frost_temp": "Lowest temperature kept while heating is off during vacation.",
          "ac_setpoint_strategy": "\"Boost\" ramps the AC setpoint toward the device limit (legacy). \"Offset\" shifts the comfort target by at most the offsets below, letting an inverter modulate gently. TRV/underfloor heating is unaffected.",
          "ac_cool_offset_max": "At full demand, cool to this many K below the cool target.",
          "ac_heat_offset_max": "At full demand, AC-heat to this many K above the heat target.",
          "idle_off_after_minutes": "When a room stays comfortable, hold setback this long, then fully turn the device off. 0 disables (pure setback). Auto-recovers when heating/cooling is needed again.",
          "idle_setback_offset": "How far the setback lowers (heating) / raises (cooling) the device setpoint from the room target while idle. Default 2 °C.",
          "demand_control_enabled": "RoomMind drives the shared outdoor unit's demand cap from outdoor temp + aggregate room demand. Replaces any external demand automation. Off = selects untouched (default).",
          "demand_select_entities": "The demand-control select entities of the compressor group (one per indoor unit).",
          "demand_min": "Lowest demand cap RoomMind will set (also used when the group is idle).",
          "demand_max": "Highest demand cap RoomMind will set.",
          "demand_hysteresis": "Don't re-apply demand changes smaller than this (anti-flutter).",
          "demand_min_hold_minutes": "Minimum time between demand changes (anti short-cycle of the cap).",
          "demand_down_hold_minutes": "Anti ping-pong: only lower the demand cap after the rooms have stayed satisfied this long. Demand still rises immediately to cover heat need. 0 = off (legacy).",
          "pv_boost_enabled": "When the home produces surplus solar that would otherwise be exported, lift the demand cap so the AC can park the excess in thermal mass. Off = no effect.",
          "pv_surplus_sensor": "A sensor (Watts) that you build in HA — positive values mean exportable surplus. Typical recipe: invert the grid-meter sign, or PV − house load. Empty = boost off.",
          "pv_surplus_min_w": "Surplus must exceed this for the boost to engage.",
          "pv_surplus_min_duration_minutes": "Sustained-on duration before boost fires (anti-flap). Drop-off is immediate.",
          "pv_battery_soc_sensor": "Optional. When set, boost only fires once the home battery is full enough — keeps charge priority on the battery first.",
          "pv_battery_soc_min": "Minimum battery state-of-charge for boost to engage. Ignored when no SoC sensor is set.",
          "pv_boost_cool_percent": "Demand %-points added on top of base + trim while the group is actively cooling.",
          "pv_boost_heat_percent": "Demand %-points added on top of base + trim while the group is actively heating (useful in shoulder season)."
        }
      }
    }
  },
  "selector": {
    "vacation_action": {
      "options": {
        "setback": "Setback (heat to vacation temperature)",
        "off": "Off (frost protection only, cooling continues)"
      }
    },
    "ac_setpoint_strategy": {
      "options": {
        "boost": "Boost (legacy ramp toward device limit)",
        "offset": "Offset (gentle, inverter-friendly)"
      }
    }
  },
  "issues": {
    "restart_required": {
      "title": "Restart required",
      "fix_flow": {
        "step": {
          "confirm_restart": {
            "title": "Restart Home Assistant?",
            "description": "RoomMind {version} has been installed but Home Assistant is still running an older version. Restart to complete the update."
          }
        }
      }
    }
  }
}
